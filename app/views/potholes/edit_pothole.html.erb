<%= provide(:title, 'Edit Name')%>
<main role ='main' class='container'>

	<h1> Edit Pothole</h1>

	<%= render 'form_edit', pothole: @pothole%>

	<p>
	<%= link_to 'Back', home_path, data: { turbolinks: false } %>
	</p>

</main>